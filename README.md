## Ditto Ambassador

Backend Spring Boot nằm giữa `client-aplication` và Eclipse Ditto được đặt tại [`ditto-ambassador`](ditto-ambassador/README.md). Service proxy `/api/2/**` và cung cấp API create-only để chặn tạo Digital Twin trùng `thingId`.
